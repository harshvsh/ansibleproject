apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
 name: "{{ wordpress_name }}"
spec:
 scaleTargetRef:
   apiVersion: apps/v1
   kind: Deployment
   name: "{{ wordpress_name }}"
 minReplicas: 1
 maxReplicas: 10
 targetCPUUtilizationPercentage: 50
